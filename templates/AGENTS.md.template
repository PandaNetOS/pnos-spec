# <仓库名> AGENTS.md

> 本文件是 AI 代理进入本仓库时的首读指南。
> 生态级全局约束（架构、合规、工作目录、模块设计原则等）请参考 [根目录 AGENTS.md](../AGENTS.md)。

## 仓库定位

<一句话说明仓库在生态中的角色和层级>

## 架构概览

<架构图或模块列表，说明核心模块和数据流>

## 目录结构

```
<仓库名>/
├── src/                    # 源代码
│   ├── main.rs             # 入口
│   └── ...
├── config/                 # 配置文件
├── docs/                   # 文档
├── tests/                  # 测试
└── Cargo.toml              # 依赖配置
```

## 构建与测试

| 命令 | 说明 |
|---|---|
| `cargo build --release` | Release 构建 |
| `cargo test --all` | 运行所有测试 |
| `cargo fmt --all -- --check` | 格式检查 |
| `cargo clippy --all-targets -- -D warnings` | 静态分析 |

## 关键配置

| 配置项 | 默认值 | 说明 |
|---|---|---|
| <端口> | <值> | <用途> |
| <配置文件> | <路径> | <说明> |

## 依赖关系

- **依赖的标准库**：`pnos`（pnos-spec）、`pnos-sdk` 子 crate
- **被谁依赖**：<哪些项目依赖本仓库>

## 注意事项

<仓库特定的约束、坑、不可触碰的部分>

## 变更历史

| 日期 | 版本 | 变更内容 |
|---|---|---|
| 2026-09-16 | v1.0 | 初始版本 |
